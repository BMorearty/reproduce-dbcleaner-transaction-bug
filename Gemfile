source 'http://rubygems.org'

gem 'rails', '3.1.0'
gem 'sqlite3'
gem 'json'
gem 'ruby-debug19'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails', "  ~> 3.1.0"
  gem 'coffee-rails', "~> 3.1.0"
  gem 'uglifier'
end

gem 'jquery-rails'

group :test do
  gem 'database_cleaner', :git => 'git://github.com/bmabey/database_cleaner.git', :branch => 'rework_ar_connections'
end
